
  # ASR Model Evaluation SPA

  This is a code bundle for ASR Model Evaluation SPA. The original project is available at https://www.figma.com/design/e4poCHeQjfPSPDxwG0jRL0/ASR-Model-Evaluation-SPA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  